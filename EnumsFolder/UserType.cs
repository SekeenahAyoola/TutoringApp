namespace TutoringManagementApp.EnumsFolder
{
    public enum UserType
    {//REMEMBER TO USE ROLE 
    // we don't call a static class using an instance but rather using the class directly

        Tutor = 1,
        Tutee = 2,
        Admin = 3,
        SuperAdmin = 4,

        Unknown = 5
    }
}